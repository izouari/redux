import React from 'react'

const User = (props) => {
    return (
        <div>
            <h1>The User Page</h1>
             {/* Name : {props.user.name}
             <br></br>
             Age  : {props.user.age} */}
        </div>
    )
}

export default User
