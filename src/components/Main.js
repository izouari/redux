import React from 'react'

const Main = (props) => {
    
    return (
        <div>

            <h1>Init User</h1>
            {/* <h1>Name : {props.user.name}</h1>
            <h1>Age : {props.user.age}</h1>
            <button onClick={() => props.onCLick('BBB', 22)}>Change Name</button>
             */}
        </div>
    )
}

export default Main
